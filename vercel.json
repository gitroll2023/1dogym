{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install && prisma generate",
        "buildCommand": "npm run vercel-build"
      }
    }
  ]
}
